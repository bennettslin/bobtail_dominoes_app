import { addTurn, getInitialTurns } from '.'

describe('getInitialTurns', () => {
    // it('generates turns for one hand', () => {
    //     expect(getInitialTurns(1)).toStrictEqual([0])
    // })

    // it('generates scores for four hands', () => {
    //     expect(getInitialTurns(4)).toStrictEqual([0, 0, 0, 0])
    // })
})

describe('addTurn', () => {
    // it('adds score to one hand', () => {
    //     expect(addTurn({
    //         playerIndex: 0,
    //         points: 5,
    //         scores: [17],
    //     })).toStrictEqual([22])
    // })
})
