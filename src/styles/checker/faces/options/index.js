/* eslint-disable key-spacing */
import { hsl } from '../../../../utils/svgs'

export default {
    fill: {
        optionFace:     hsl(200, 35, 62),
        optionBottom:   hsl(200, 35, 72),

        resetFace:      hsl(320, 20, 62),
        resetBottom:    hsl(320, 20, 72),
    },
}
